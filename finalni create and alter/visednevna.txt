CREATE TABLE VISEDNEVNA
(
    "ID_ULAZNICE" NUMBER,
    "BROJ_DANA" NUMBER
);

//keys i null//

ALTER TABLE VISEDNEVNA MODIFY ("ID_ULAZNICE" NOT NULL);
ALTER TABLE VISEDNEVNA MODIFY ("BROJ_DANA" NOT NULL);
ALTER TABLE VISEDNEVNA ADD CONSTRAINT "VISEDNEVNA_FK1" FOREIGN KEY ("ID_ULAZNICE")
    REFERENCES ULAZNICA ("ID_ULAZNICE") ON DELETE CASCADE;

//constraint//

ALTER TABLE VISEDNEVNA ADD CONSTRAINT 
"CHK_VISE_BROJ_DANA" CHECK (BROJ_DANA > 1);